<!-- Navbar start -->
<nav id="barNav" class="navbar navbar-expand-lg bg-info-subtle">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <div class="container border p-3 ms-0 w-auto">
                Logo
            </div>
            <div>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="navbar-brand" href="{{ path('app_home') }}">Home</a>
                    </li>
                    {% if app.user %}
                    <li class="nav-item">
                        <p class="navbar-brand">|</p>
                    </li>
                    <li class="nav-item">
                        <a class="navbar-brand" href="{{ path('app_new') }}">Tricks</a>
                    </li>
                    {% endif %}
                    <li class="nav-item">
                        <p class="navbar-brand">|</p>
                    </li>
                    {% if not app.user %}
                        <li class="nav-item">
                            <a class="navbar-brand" href="{{ path('app_login') }}">Sign in</a>
                        </li>
                        <li class="nav-item">
                            <p class="navbar-brand">|</p>
                        </li>
                        <li class="nav-item">
                            <a class="navbar-brand" href="{{ path('app_register') }}">Sign up</a>
                        </li>
                        <li class="nav-item">
                            <p class="navbar-brand">|</p>
                        </li>
                    {% endif %}
                    {% if app.user %}
                        <li class="nav-item">
                            <a class="navbar-brand" href="{{ path('app_logout') }}">Logout</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </div>
</nav>
<!-- Navbar end -->